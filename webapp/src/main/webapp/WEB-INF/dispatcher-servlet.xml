<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context-4.0.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Spring annotated component scan -->
    <context:component-scan base-package="com.zakangroth.demo"/>

    <!-- Process annotation on registered beans (ex : @Autowired) -->
    <context:annotation-config/>

    <!-- Retrieves DefaultAnnotationHandlerMapping and AnnotationMethodHandlerAdapter needed for SpringMVC -->
    <mvc:annotation-driven/>

    <!-- View resolver for JSP files-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--
        Resolve the Locale from the session. If not defined : AcceptHeaderLocaleResolver is used by default.
        AcceptHeaderLocaleResolver takes the "accept-language" HTTP header as the Locale.
    -->
    <bean id="localeResolver"
          class="org.springframework.web.servlet.i18n.SessionLocaleResolver">
        <property name="defaultLocale" value="fr"/>
    </bean>

    <!-- Language files mapping (messages_xx.properties)-->
    <bean id="messageSource"
          class="org.springframework.context.support.ResourceBundleMessageSource">
        <!-- Locale source file prefix -->
        <property name="basename" value="messages"/>
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

    <mvc:interceptors>
        <bean id="localeChangeInterceptor"
              class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor">
            <property name="paramName" value="lang"/>
        </bean>
    </mvc:interceptors>

    <mvc:resources mapping="/resources/**" location="./resources/"/>
</beans>